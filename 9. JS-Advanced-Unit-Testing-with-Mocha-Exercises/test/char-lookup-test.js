let charLookup = require("../char-lookup").lookupChar;
let expect = require("chai").expect;

/*describe("CharLookup", function () {
    it("with non-string first param should return undefined", function () {
        expect(charLookup(13, 0)).to.equal(undefined);
    });
    it("with non-number second param should return indefined", function () {
        expect(charLookup("pesho", "gosho")).to.equal(undefined);
    });
    it("with non-integer second param should return undefined", function () {
        expect(charLookup("pesho", 3.14)).to.equal(undefined);
    });
    it("with negative number second param should return incorrect index", function () {
        expect(charLookup("pesho", -2)).to.equal("Incorrect index");
    });
    it("with index bigger than string.length should return incorrect index", function () {
        expect(charLookup("pesho", 10)).to.equal("Incorrect index");
    });
    it("with correct parameter should return correct output", function () {
        expect(charLookup("pesho", 0)).to.equal("p");
    });
    it("with correct parameter should return correct output", function () {
        expect(charLookup("stamat", 3)).to.equal("m");
    });
});*/
