let mathEnforcer = require("../Math-Enforcer").mathEnforcer;
let expect = require("chai").expect;

/*describe("MathEnforcer", function () {
    describe("addFive", function () {
        it("with non-number should return correct result", function () {
            expect(mathEnforcer.addFive("pesho")).to.equal(undefined);
        });
        it("with negative number should return correct result", function () {
            expect(mathEnforcer.addFive(-3)).to.equal(2);
        });
        it("with floating number should return correct result", function () {
            expect(mathEnforcer.addFive(0.01)).to.be.closeTo(5, 0.01);
        });
    });
    describe("substractTen", function () {
        it("with non-number should return correct result", function () {
            expect(mathEnforcer.subtractTen("pesho")).to.equal(undefined);
        });
        it("with negative number should return correct result", function () {
            expect(mathEnforcer.subtractTen(-3)).to.equal(-13);
        });
        it("with floating number should return correct result", function () {
            expect(mathEnforcer.subtractTen(0.01)).to.be.closeTo(-10, 0.01);
        });
    });
    describe("sum", function () {
        it("with non-number should return correct result", function () {
            expect(mathEnforcer.sum("pesho", "gosho")).to.equal(undefined);
        });
        it("with number should return correct result", function () {
            expect(mathEnforcer.sum(2,3)).to.equal(5);
        });
        it("with negative number should return correct result", function () {
            expect(mathEnforcer.sum(-3, -2)).to.equal(-5);
        });
        it("with floating number should return correct result", function () {
            expect(mathEnforcer.sum(0.1, 0.2)).to.be.closeTo(0.3, 0.01);
        });
    });
});*/
